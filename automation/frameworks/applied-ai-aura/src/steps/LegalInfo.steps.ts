/**
 * AURA — LegalInfo Steps
 * Step definitions specific to the LegalInfo feature.
 * All common steps (navigate, click, visible, URL) are in Common.steps.ts / Preview.steps.ts.
 * No new domain-specific steps are required for this feature — all behavior is covered by Common.
 */

// This file is intentionally minimal: every Gherkin step in LegalInfo.feature
// resolves to Common.steps.ts or Preview.steps.ts. If domain-specific steps are
// needed in the future, add single-line delegations to this.webActions here.
